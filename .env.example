# MeloVault Environment Variables
# Copy this file to .env and fill in the values

# App
APP_ENV=development # development | production

# Lyrics (LRCLib is free and requires no API key)
# LRCLIB_BASE_URL is hardcoded — no config needed

# EAS Build (optional — only needed for cloud builds)
# EXPO_TOKEN=your_expo_token_here
